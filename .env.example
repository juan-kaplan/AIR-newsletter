NEWSLETTER_AI_PROVIDER=none
GEMINI_API_KEY=<optional — only needed if NEWSLETTER_AI_PROVIDER=gemini>
NVIDIA_API_KEY=<optional — only needed if NEWSLETTER_AI_PROVIDER=nvidia>
NVIDIA_MODEL=meta/llama-3.1-70b-instruct
