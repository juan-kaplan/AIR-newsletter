{
  "name": "@newsletter/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "cf:migrate:local": "wrangler d1 migrations apply <D1_DATABASE_NAME> --local",
    "cf:migrate:remote": "wrangler d1 migrations apply <D1_DATABASE_NAME> --remote",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "wrangler": "^4.50.0"
  }
}
